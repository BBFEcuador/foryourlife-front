export interface ContificoConfigRequest {
    id?:string;
    campusId: string;
    apiKey: string;
    apiSecret: string;
}
export interface ContificoConfig {
    id:string;
    campusId: string;
    apiKey: string;
    apiSecret: string;
}